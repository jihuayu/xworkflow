# Streaming: Debug mode captures streaming events.
# File: Runtime state: engine config, mock server, fake time/id, etc.

[engine_config]
enable_debug = true

[[llm_mock]]
model = "gpt-3.5-turbo"
stream_chunks = ["Streaming ", "response"]
