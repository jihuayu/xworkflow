# Template Jinja2 conditional: template uses {% if %} conditional logic.
# File: Expected execution result and output values

status = "completed"
partial_match = false

[outputs]
result = "You have 5 items"
