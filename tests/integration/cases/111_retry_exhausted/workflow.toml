# Retry exhausted: all retry attempts are consumed, node fails.
# File: Workflow DSL definition

version = "0.1.0"
[[nodes]]
id = "start"

[nodes.data]
type = "start"
title = "Start"
variables = []
[[nodes]]
id = "http1"

[nodes.data]
type = "http-request"
title = "Retry Exhausted"
method = "GET"
url = "{{#sys.base_url#}}/api/retry"
timeout = 10
fail_on_error_status = true
[nodes.data.retry_config]
max_retries = 1
retry_interval = 0
[[nodes]]
id = "end"

[nodes.data]
type = "end"
title = "End"
[[nodes.data.outputs]]
variable = "status_code"
value_selector = [ "http1", "status_code",]

[[edges]]
source = "start"
target = "http1"

[[edges]]
source = "http1"
target = "end"

