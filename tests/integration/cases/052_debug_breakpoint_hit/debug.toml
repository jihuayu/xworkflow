# Debug breakpoint hit: debugger pauses when a breakpoint is reached.
# File: Debug mode configuration: breakpoints and step expectations

[[steps]]
expect_node = "code_1"
expect_reason = "breakpoint"
expect_phase = "before"
action = "continue"

[config]
breakpoints = [ "code_1",]
break_on_start = false
auto_snapshot = false
