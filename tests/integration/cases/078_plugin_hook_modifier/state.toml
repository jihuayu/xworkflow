# Plugin hook modifier: plugin hook modifies data during workflow execution.
# File: Runtime state: engine config, mock server, fake time/id, etc.

[plugin_system]
host_normal_plugins = [ "test_hook_modifier",]
