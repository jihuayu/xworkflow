# Plugin node basic: invokes a basic host-registered plugin node.
# File: Runtime state: engine config, mock server, fake time/id, etc.

[plugin_system]
host_bootstrap_plugins = [ "wasm_bootstrap",]
[[plugin_system.normal_load_sources]]
loader_type = "wasm"

[plugin_system.normal_load_sources.params]
dir = "plugins/com.example.echo"
