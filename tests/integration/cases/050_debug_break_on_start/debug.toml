# Debug break on start: debugger pauses at the very first node before execution.
# File: Debug mode configuration: breakpoints and step expectations

[[steps]]
expect_node = "start"
expect_reason = "initial"
expect_phase = "before"
action = "continue"

[config]
breakpoints = []
break_on_start = true
auto_snapshot = false
